<%= form_with model: @kitten do |f| %>
  <% if @kitten.errors.any? %>
    <div>
      <h2><%= pluralize(@kitten.errors.count, "error") %> 
      prohibited the kitten from being saved</h2>
    </div>

    <ul>
      <% @kitten.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
  <% end %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div>
    <%= f.label :cuteness %>
    <%= f.number_field :cuteness %>
  </div>

  <div>
    <%= f.label :softness %>
    <%= f.text_field :softness %>
  </div>

  <%= f.submit :Submit %>

<% end %>